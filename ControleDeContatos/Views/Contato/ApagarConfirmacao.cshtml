@model ContatoModel
@{
    ViewData["Title"] = "Confirmação de apagar contato";
}

<div class="text-center">
    <p>Realmente deseja apagar o contato (@Model.Nome)?</p>

    <a role="button" class="btn btn-primary" asp-controller="Contato" asp-action="Index">
        Cancelar exclusão
    </a>
    <a role="button" class="btn btn-danger" asp-route-id="@Model.Id" asp-controller="Contato" asp-action="Apagar">
        Confirmar exclusão
    </a>
</div>